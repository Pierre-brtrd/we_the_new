<form action="{{ path('admin.genders.delete', {id: gender.id}) }}" method="POST" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce genre ?')">
	<input type="hidden" name="token" value="{{ csrf_token('delete' ~ gender.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
