{{ form_start(form, {attr: {class: 'card w-50 mx-auto mt-4'}}) }}
{{ form_widget(form) }}
<button type="submit" class="btn btn-primary">{{ buttonLabel|default('save') }}</button>
{{ form_end(form) }}
