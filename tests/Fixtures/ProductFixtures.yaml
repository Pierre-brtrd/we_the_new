include:
    - ModelFixtures.yaml

App\Entity\Product\Product:
    product_{1..5}:
        name (unique): <word()>
        description: <sentence()>
        model: "@model_<current()>"
        enable: true
